language: python
python:
  - "3.8"

install:
  - pip install -r requirements.txt

script:
  - python -m pytest tests/

deploy:
  provider: script
  script: ansible-playbook ansible/playbook.yml
  on:
    branch: main